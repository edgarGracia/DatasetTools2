defaults:
  - _self_
  - visualization
  - task: pass
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog
  
dataset:
  parser: null    # Parser class name from ``DatasetTools.data_parser``
  labels: {}
  annotations_path: null
  images_path: null
  meta: {}

hydra:
  output_subdir: null
  run:  
    dir: .
