defaults:
  - _self_
  - visualization
  - task: null
  - dataset: coco
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog

hydra:
  output_subdir: null
  run:  
    dir: .
